all:
  children:
    web_servers:
      hosts:
        web-server-1:
          ansible_host: web-1
          ansible_connection: docker
        web-server-2:
          ansible_host: web-2
          ansible_connection: docker
      vars:
        ansible_user: root
        ansible_python_interpreter: /usr/bin/python3