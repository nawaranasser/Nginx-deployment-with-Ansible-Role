server {
    listen {{ nginx_port }};
    listen [::]:{{ nginx_port }};
    
    root /var/www/html;
    index index.html index.html;
    
    server_name _;
    
    location / {
        try_files $uri $uri/ =404;
    }
}